"""Built-in agent presets for focusgroup.

These presets are bundled with the package and provide useful starting points
for common agent roles (UX reviewer, security reviewer, etc.).

User presets in ~/.config/focusgroup/agents/ take precedence over
bundled presets with the same name.
"""
